import React from "react";
import { Typography, Card } from "antd";

const UserManagementPage = () => {
  return (
    <Card>
      <Typography.Title level={2}>Quản lý User</Typography.Title>
      <Typography.Text>Manage user data and permissions here.</Typography.Text>
      {/* Add more content here */}
    </Card>
  );
};

export default UserManagementPage;
